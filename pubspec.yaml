name: dart_express
description: A new way for dart into web servers world.
version: 0.0.1
homepage: https://github.com/amrmhassan/dart_express

environment:
  sdk: '>=2.19.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  logger: ^1.3.0
  mime: ^1.0.4
  path: ^1.8.2
  uuid: ^3.0.7


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
